<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- NOTE: LSUIElement removed - dock visibility controlled programmatically via setActivationPolicy -->
    <!-- App starts in accessory mode (no dock) and shows dock icon when main window opens -->

    <!-- App info -->
    <key>CFBundleName</key>
    <string>Great Deploy</string>

    <key>CFBundleDisplayName</key>
    <string>Great Deploy</string>

    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>

    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>

    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>

    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <!-- Minimum macOS version -->
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>

    <!-- App category -->
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.developer-tools</string>

    <!-- Copyright -->
    <key>NSHumanReadableCopyright</key>
    <string>Copyright © 2024. All rights reserved.</string>

    <!-- Keychain usage description -->
    <key>NSKeychainUsageDescription</key>
    <string>Great Deploy needs access to Keychain to manage your GitHub credentials securely.</string>
</dict>
</plist>
